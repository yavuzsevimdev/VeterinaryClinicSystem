# avatars/

Place real user/owner avatar images here.

Until real photos exist, the `.avatar` component (see `.avatar` in [css/topbar.css](../../css/topbar.css))
renders initials on a gradient background as a placeholder. To use a photo, drop an `<img>` inside
the `.avatar` element pointing at a file in this folder — the component already clips it to a circle.
